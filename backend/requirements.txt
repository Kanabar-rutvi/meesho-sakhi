# Meesho Sakhi Backend Dependencies
# ==================================
# Install with: pip install -r requirements.txt

# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
# For production, consider: psycopg2-binary (PostgreSQL driver)

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Authentication & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.1.1

# AI & LLM
anthropic==0.7.1

# Utilities
python-dotenv==1.0.0
requests==2.31.0

# Optional: For production deployments
# gunicorn==21.2.0  # Production WSGI server (use with uvicorn worker)
# psycopg2-binary==2.9.9  # PostgreSQL database driver (if using PostgreSQL)
