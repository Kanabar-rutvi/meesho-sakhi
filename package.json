{
  "name": "meesho-sakhi",
  "version": "2.0.0",
  "private": true,
  "description": "Meesho Sakhi — AI Shopping Companion Monorepo",
  "scripts": {
    "install:backend": "cd backend && npm install --no-audit --no-fund",
    "install:frontend": "cd frontend && npm install --no-audit --no-fund",
    "install:all": "npm run install:backend && npm run install:frontend",
    "build:backend": "cd backend && npx prisma generate",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:backend && npm run build:frontend",
    "lint:backend": "cd backend && npm run lint",
    "lint:frontend": "cd frontend && npm run lint",
    "lint": "npm run lint:backend && npm run lint:frontend"
  },
  "engines": {
    "node": "20.x"
  }
}
